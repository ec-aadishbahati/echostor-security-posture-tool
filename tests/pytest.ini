[pytest]
testpaths = ui_tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --headed
    --slowmo=500
    --screenshot=only-on-failure
    --video=retain-on-failure
